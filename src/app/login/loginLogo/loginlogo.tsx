import React from "react";

const loginlogo = (): JSX.Element => {
    return(
        <div>
            <span className="Login-Logo">
                로그인
            </span>
        </div>
    );
};

export default loginlogo;