import React from 'react';

interface AccountInfoPresenterProps {
	userId: string;
}

const AccountInfoPresenter = ({ userId }: AccountInfoPresenterProps): JSX.Element => {
	return <div>AccountInfoPresenter</div>;
};

export default AccountInfoPresenter;
